# vim: ft=sh

# Promptを変更
prompt redhat
PROMPT="%B%F{cyan}[%n@%m %1~]%b%f%(#.#.$) "

# プロンプトにブランチ名称を表示
precmd_vcs_info() { vcs_info }
chpwd() { ls }
precmd_functions+=( precmd_vcs_info )
setopt prompt_subst
RPROMPT=\$vcs_info_msg_0_
zstyle ':vcs_info:git:*' formats '%b'
